name: spellcheck

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: sbates130272/spellcheck-github-actions@v0.7.0
        name: Spellcheck
